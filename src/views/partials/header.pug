header 
  h1 
    a(href="/").material-icons nights_staySW
  
  
  nav 
    ul.nav
      li
        a(href="/search").material-icons search
        
      if loggedIn
        li.login
          if loggedInUser.avatarUrl === ""
            span 😀
          else
            img.header__avatar(src="/" + loggedInUser.avatarUrl)
            span #{loggedInUser.name} 님.
              i.material-icons expand_more
        li 
          a(href="").material-icons notifications
      else
        li 
          a(href="/join").material-icons person_add
        li 
          a(href="/login").material-icons login
          
    ul.drop-menu
      li 
        a(href="/videos/upload")
          span.material-icons upload
          | Upload Video
      li 
        a(href=`/users/${loggedInUser._id}`)
          span.material-icons account_circle 
          | My Channel
      li
        a(href="/users/edit")
          span.material-icons manage_accounts
          | Edit Profile
      li 
        a(href="/users/logout")
          span.material-icons logout 
          | Logout
  //- div.hamburger
  //-   span 
  //-   span 
  //-   span
div.overlay
script(src="/static/js/main.js")